<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="shop.mtcoding.project.model.UserRepository">
    <select id="findAll" resultType="shop.mtcoding.project.model.User">
        select * from user_tb
    </select>

    <select id="findById" resultType="shop.mtcoding.project.model.User">
        select * from user_tb id = #{uDto.id}
    </select>


    <select id="findByUserEmail" resultType="shop.mtcoding.project.model.User">
        select * from user_tb where email = #{email}
    </select>

    <select id="findByEmailAndPassword" resultType="shop.mtcoding.project.model.User">
        select * from user_tb where email = #{email} and password = #{password}
    </select>

    <select id="findByUserSkill" resultType="shop.mtcoding.project.dto.user.UserResp$UserSkillRespDto">
        select s.skill_name1, s.skill_name2, s.skill_name3 from user_tb u join USER_SKILL_TB s 
            on u.user_id = s.user_id
            where u.user_id = #{id}
    </select>

    <select id="findByUserSkillAndInterest" resultType="shop.mtcoding.project.dto.user.UserResp$UserSkillAndInterestDto">
        SELECT s.skill_name1, s.skill_name2, s.skill_name3, i.interest_ct1, i.interest_ct2, i.interest_ct3 
            FROM USER_TB u 
            JOIN interest_tb i ON u.user_id = i.user_id
            JOIN user_skill_tb s ON u.user_id = s.user_id
            WHERE u.user_id = #{id}

    </select>

    <insert id="insert">
        insert into user_tb ( email, password, name, birth, tel, address, created_at)
            values ( #{uDto.email}, #{uDto.password}, #{uDto.name}, #{uDto.birth}, #{uDto.tel}, #{uDto.Address}, now());
    </insert>

    <update id="updateById">
        update user_tb set 
            password =  #{uDto.password}, 
            name = #{uDto.name}, 
            birth = #{uDto.birth},
            tel =  #{uDto.tel},
            Address = #{uDto.Address}, 
            where id = #{uDto.id}
    </update>

    <delete id="deleteById">
        delete from user_tb where id = #{uDto.id}
    </delete>
</mapper>