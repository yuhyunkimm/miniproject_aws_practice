<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="shop.mtcoding.project.model.CompRepository">
    <select id="findAll" resultType="shop.mtcoding.project.model.Comp">
        select * from comp_tb
    </select>

    <select id="findById" resultType="shop.mtcoding.project.model.User">

    </select>

    <insert id="insert">
      insert into comp_tb ( email, password, compName, representativeName, businessNumber, created_at)
            values ( #{uDto.email}, #{uDto.password}, #{uDto.compName}, #{uDto.representativeName}, #{uDto.businessNumber}, now());
    </insert>

    <update id="updateById">

    </update>

    <delete id="deleteById">

    </delete>
</mapper>